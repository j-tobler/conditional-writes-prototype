proc simple {
	x := y;
}

proc multi_statement {
    x := y;
    y := z;
    abc := 123;
}

proc conditionals {
    x := y;
    if x == y {
        x := 5;
    } else {
        x := 6;
    }
    if x == y {
        x := 7;
    }
    if 1 {} else {
        x := 5;
    }
}

proc complicated_expression {
    x := 5 <==> 3 <==> x ==> 5 + 3 * 3 % 3 || y && (z ==> 2 < 3 < 4);
}

proc loops {
    while 1 < x <= 5 {
        assume x < y;
        y := z - -(-x - -z);
    }
}
